# Figures

The translation-pipeline figure is native TikZ so it remains sharp in print,
works in grayscale, and does not require a binary image asset. It is included
from `parts/part01.tex`, which is regenerated by
`tools/generate_book.py`.

